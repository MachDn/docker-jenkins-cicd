<!DOCTYPE html>

<html>

    <head>
        <title>good cicd practice</title>
        <style>
            a {
                text-decoration: none;
                color: black;
            }
	    body {
  font-family: Arial, sans-serif;
  background-color: #f2f2f2;
}

h1 {
  color: #333;
  font-size: 36px;
  font-weight: bold;
  text-align: center;
  margin-top: 50px;
}

ul {
  list-style: none;
  margin: 0;
  padding: 0;
}

li {
  margin: 10px 0;
  padding: 10px;
  border: 1px solid #ccc;
  background-color: #fff;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

a {
  text-decoration: none;
  color: #333;
  margin-left: 10px;
  font-size: 20px;
}

a:hover {
  color: #FF5733;
}

label {
  display: block;
  margin-bottom: 10px;
  font-size: 20px;
  font-weight: bold;
}

input[type="text"] {
  padding: 10px;
  font-size: 20px;
  border: none;
  border-radius: 5px;
  width: 100%;
  box-sizing: border-box;
}

input[type="submit"] {
  padding: 10px;
  font-size: 20px;
  border: none;
  border-radius: 5px;
  background-color: #FF5733;
  color: #fff;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

input[type="submit"]:hover {
  background-color: #ff8b5a;
}

        </style>
    </head>

    <body>
        <h1> success cicd test!</h1>
        <ul>
            <% todolist.forEach(function(todo, index) { %>
            <li>
                <a href="/todo/delete/<%= index %>">✘</a>
                <a href="/todo/<%= index %>">✎</a>
                <%- todo %>
            </li>
            <% }); %>
        </ul>

        <form action="/todo/add/" method="post">
            <p>
                <label for="newtodo">What shoud I do??</label>
                <input type="text" name="newtodo" id="newtodo" autofocus />
                <input type="submit" value="Add" />
            </p>
        </form>
    </body>

</html>
